import { ProductTypeUpdate } from '../product-type/product-type-update';

describe('ProductTypeUpdate', () => {
  it('should create an instance', () => {
    expect(new ProductTypeUpdate()).toBeTruthy();
  });
});
