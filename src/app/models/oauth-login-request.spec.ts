import { OauthLoginRequest } from './oauth-login-request';

describe('OauthLoginRequest', () => {
  it('should create an instance', () => {
    expect(new OauthLoginRequest()).toBeTruthy();
  });
});
